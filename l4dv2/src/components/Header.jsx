import React from "react";
const Header =() => {
    return (<header id="header">
        <div className="header-overlay">
          Header
        </div>
    </header>
    );
};

export default Header;